name: Build and Deploy

on:
  push:
    branches:
      - main
  workflow_dispatch:

jobs:
  build-and-deploy:
    runs-on: ubuntu-latest

    steps:
      - name: Checkout
        uses: actions/checkout@v4

      - name: Setup Node.js
        uses: actions/setup-node@v4
        with:
          node-version: '20'
          cache: 'npm'

      - name: Install dependencies
        run: npm ci

      - name: Create .env file for build
        run: |
          echo "API_AUTH_EMAIL=${{ secrets.API_AUTH_EMAIL }}" >> .env
          echo "API_AUTH_PASSWORD=${{ secrets.API_AUTH_PASSWORD }}" >> .env
          echo "API_URL=${{ secrets.API_URL }}" >> .env

      - name: Build (SSR mode)
        run: npm run build

      - name: Prepare deployment
        run: |
          mkdir -p deploy
          cp -r .output deploy/.output
          cp package.json deploy/
          cp package-lock.json deploy/ || true
          cat > deploy/.env << EOF
          NUXT_API_URL=${{ secrets.API_URL }}
          NUXT_API_AUTH_EMAIL=${{ secrets.API_AUTH_EMAIL }}
          NUXT_API_AUTH_PASSWORD=${{ secrets.API_AUTH_PASSWORD }}
          NUXT_PUBLIC_CMS_BASE_URL=${{ secrets.API_URL }}
          EOF

      - name: Install sshpass
        run: sudo apt-get install -y sshpass

      - name: Create target directory on server
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" ssh -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=no \
            ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }} \
            "mkdir -p ${{ secrets.SSH_TARGET_DIR }}"

      - name: Deploy via rsync with password
        run: |
          sshpass -p "${{ secrets.SSH_PASSWORD }}" rsync -avz --delete \
            -e "ssh -p ${{ secrets.SSH_PORT }} -o StrictHostKeyChecking=no" \
            deploy/ \
            ${{ secrets.SSH_USERNAME }}@${{ secrets.SSH_HOST }}:${{ secrets.SSH_TARGET_DIR }}
